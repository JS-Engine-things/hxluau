name: Build Luau Libraries

on: workflow_dispatch

permissions:
  contents: write

jobs:
  build-libs:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            platform: Linux
            arch: x64
            lib_name: libluau.a
          - os: ubuntu-22.04
            platform: Linux
            arch: arm64
            lib_name: libluau.a
          - os: ubuntu-22.04
            platform: Linux
            arch: x86
            lib_name: libluau.a
          - os: ubuntu-22.04
            platform: Linux
            arch: armv7
            lib_name: libluau.a
          - os: windows-latest
            platform: Windows
            arch: x64
            lib_name: luau.lib
            msvc_arch: x64
          - os: windows-latest
            platform: Windows
            arch: arm64
            lib_name: luau.lib
            msvc_arch: amd64_arm64
          - os: windows-latest
            platform: Windows
            arch: x86
            lib_name: luau.lib
            msvc_arch: x86
          - os: macos-latest
            platform: MacOS
            arch: x64
            lib_name: libluau.a
          - os: macos-latest
            platform: MacOS
            arch: arm64
            lib_name: libluau.a
          - os: ubuntu-latest
            platform: MinGW
            arch: x64
            lib_name: libluau.a
          - os: ubuntu-latest
            platform: MinGW
            arch: x86
            lib_name: libluau.a

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup CMake
        if: matrix.os == 'windows-latest'
        uses: jwlawson/actions-setup-cmake@v1.14
        with:
          cmake-version: '3.22.x'

      - name: Setup MSVC (Windows)
        if: matrix.msvc_arch
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: ${{ matrix.msvc_arch }}

      - name: Clone Luau repository
        run: |
          rm -rf project/luau || true
          git clone https://github.com/luau-lang/luau.git project/luau
        shell: bash

      - name: Build Luau library (Linux/macOS)
        if: matrix.platform == 'Linux' || matrix.platform == 'MacOS'
        run: |
          cd project/luau
          mkdir -p build
          cd build

          if [ "${{ matrix.platform }}" = "MacOS" ]; then
            if [ "${{ matrix.arch }}" = "x64" ]; then
              cmake .. -G "Unix Makefiles" -DCMAKE_BUILD_TYPE=MinSizeRel -DCMAKE_OSX_ARCHITECTURES=x86_64 -DLUAU_BUILD_TESTS=OFF -DLUAU_BUILD_CLI=OFF -DCMAKE_EXE_LINKER_FLAGS="-framework CoreFoundation"
            else
              cmake .. -G "Unix Makefiles" -DCMAKE_BUILD_TYPE=MinSizeRel -DCMAKE_OSX_ARCHITECTURES=arm64 -DLUAU_BUILD_TESTS=OFF -DLUAU_BUILD_CLI=OFF -DCMAKE_EXE_LINKER_FLAGS="-framework CoreFoundation"
            fi
          else
            if [ "${{ matrix.arch }}" = "arm64" ]; then
              sudo apt-get update
              sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu binutils-aarch64-linux-gnu cmake ninja-build
              cmake .. -G "Ninja" -DCMAKE_BUILD_TYPE=MinSizeRel -DLUAU_BUILD_TESTS=OFF -DLUAU_BUILD_CLI=OFF \
                -DCMAKE_C_COMPILER=aarch64-linux-gnu-gcc \
                -DCMAKE_CXX_COMPILER=aarch64-linux-gnu-g++ \
                -DCMAKE_AR=/usr/bin/aarch64-linux-gnu-ar \
                -DCMAKE_RANLIB=/usr/bin/aarch64-linux-gnu-ranlib
            elif [ "${{ matrix.arch }}" = "x86" ]; then
              sudo apt-get update
              sudo apt-get install -y gcc-i686-linux-gnu g++-i686-linux-gnu binutils-i686-linux-gnu cmake ninja-build
              cmake .. -G "Ninja" -DCMAKE_BUILD_TYPE=MinSizeRel -DLUAU_BUILD_TESTS=OFF -DLUAU_BUILD_CLI=OFF \
                -DCMAKE_C_COMPILER=i686-linux-gnu-gcc \
                -DCMAKE_CXX_COMPILER=i686-linux-gnu-g++ \
                -DCMAKE_AR=/usr/bin/i686-linux-gnu-ar \
                -DCMAKE_RANLIB=/usr/bin/i686-linux-gnu-ranlib
            elif [ "${{ matrix.arch }}" = "armv7" ]; then
              sudo apt-get update
              sudo apt-get install -y gcc-arm-linux-gnueabihf g++-arm-linux-gnueabihf binutils-arm-linux-gnueabihf cmake ninja-build
              cmake .. -G "Ninja" -DCMAKE_BUILD_TYPE=MinSizeRel -DLUAU_BUILD_TESTS=OFF -DLUAU_BUILD_CLI=OFF \
                -DCMAKE_C_COMPILER=arm-linux-gnueabihf-gcc \
                -DCMAKE_CXX_COMPILER=arm-linux-gnueabihf-g++ \
                -DCMAKE_AR=/usr/bin/arm-linux-gnueabihf-ar \
                -DCMAKE_RANLIB=/usr/bin/arm-linux-gnueabihf-ranlib
            else
              cmake .. -G "Ninja" -DCMAKE_BUILD_TYPE=MinSizeRel -DLUAU_BUILD_TESTS=OFF -DLUAU_BUILD_CLI=OFF
            fi
          fi

          cmake --build . --config MinSizeRel

          echo "Listing libLuau.*.a recursively:"
          find . -name 'libLuau.Ast.a'      -type f -print || true
          find . -name 'libLuau.Bytecode.a'  -type f -print || true
          find . -name 'libLuau.Common.a'    -type f -print || true
          find . -name 'libLuau.Compiler.a'  -type f -print || true
          find . -name 'libLuau.Config.a'    -type f -print || true
          find . -name 'libLuau.VM.a'        -type f -print || true
          find . -name 'libLuau.CodeGen.a'   -type f -print || true

          if [ "${{ matrix.platform }}" = "MacOS" ]; then
            libs=$( (
              find . -name 'libLuau.Ast.a'     -type f
              find . -name 'libLuau.Bytecode.a' -type f
              find . -name 'libLuau.Common.a'   -type f
              find . -name 'libLuau.Compiler.a' -type f
              find . -name 'libLuau.Config.a'   -type f
              find . -name 'libLuau.VM.a'       -type f
              find . -name 'libLuau.CodeGen.a'  -type f
            ) | tr '\n' ' ' )
            if [ -z "$libs" ]; then
              echo "No libLuau.*.a found"; exit 1
            fi
            libtool -static -o libluau.a $libs
            ranlib libluau.a
          else
            if [ "${{ matrix.arch }}" = "arm64" ]; then
              AR_BIN=aarch64-linux-gnu-ar
              RANLIB_BIN=aarch64-linux-gnu-ranlib
            elif [ "${{ matrix.arch }}" = "x86" ]; then
              AR_BIN=i686-linux-gnu-ar
              RANLIB_BIN=i686-linux-gnu-ranlib
            elif [ "${{ matrix.arch }}" = "armv7" ]; then
              AR_BIN=arm-linux-gnueabihf-ar
              RANLIB_BIN=arm-linux-gnueabihf-ranlib
            else
              AR_BIN=ar
              RANLIB_BIN=ranlib
            fi
            echo "create libluau.a" > combine.mri
            find . -name 'libLuau.Ast.a'      -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
            find . -name 'libLuau.Bytecode.a'  -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
            find . -name 'libLuau.Common.a'    -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
            find . -name 'libLuau.Compiler.a'  -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
            find . -name 'libLuau.Config.a'    -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
            find . -name 'libLuau.VM.a'        -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
            find . -name 'libLuau.CodeGen.a'   -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
            echo "save" >> combine.mri
            echo "end" >> combine.mri
            $AR_BIN -M < combine.mri
            $RANLIB_BIN libluau.a
          fi

          mkdir -p ../built_lib
          cp libluau.a ../built_lib/libluau.a || true

          mkdir -p ../archives
          if [ -d ../built_lib ] && [ "$(ls -A ../built_lib || true)" ]; then
            tar -C ../built_lib -czf ../archives/luau-libs-${{ matrix.platform }}-${{ matrix.arch }}.tar.gz .
            echo "Created archive: ../archives/luau-libs-${{ matrix.platform }}-${{ matrix.arch }}.tar.gz"
            ls -la ../archives
          else
            echo "ERROR: built_lib empty or missing"; ls -la .. || true; exit 1
          fi
        shell: bash

      - name: Validate Unix library (Linux/macOS)
        if: matrix.platform == 'Linux' || matrix.platform == 'MacOS'
        run: |
          set -e
          cd project/luau/built_lib
          if [ ! -f "libluau.a" ]; then
            echo "ERROR: libluau.a not found after build"
            ls -la
            exit 1
          fi
          ar -t libluau.a > /dev/null || { echo "ERROR: 'ar' failed to read libluau.a"; exit 1; }
          command -v file >/dev/null 2>&1 && file -b libluau.a || true
        shell: bash

      - name: Build Luau library (Windows)
        if: matrix.platform == 'Windows'
        run: |
            rmdir /s /q project\luau 2>nul || exit /b 0
            git clone https://github.com/luau-lang/luau.git project\luau
            cd project\luau
            mkdir build
            cd build
            cmake .. -G "Ninja" -DCMAKE_BUILD_TYPE=MinSizeRel -DLUAU_STATIC_CRT=ON -DLUAU_BUILD_TESTS=OFF -DLUAU_BUILD_CLI=OFF
            cmake --build . --config MinSizeRel
            echo Listing candidate Luau.*.lib in current build dir:
            dir /b Luau.Ast.lib Luau.Bytecode.lib Luau.Common.lib Luau.Compiler.lib Luau.Config.lib Luau.VM.lib Luau.CodeGen.lib 2>nul

            setlocal EnableDelayedExpansion
            set FILES=
            for %%f in (Luau.Ast.lib Luau.Bytecode.lib Luau.Common.lib Luau.Compiler.lib Luau.Config.lib Luau.VM.lib Luau.CodeGen.lib) do (
              if exist "%%f" set FILES=!FILES! "%%f"
            )
            if "!FILES!"=="" (
              echo No libs found in current dir; searching recursively...
              for /r %%f in (Luau.Ast.lib)      do (echo %%f | findstr /i /c:"\CMakeFiles\" >nul || set FILES=!FILES! "%%f")
              for /r %%f in (Luau.Bytecode.lib)  do (echo %%f | findstr /i /c:"\CMakeFiles\" >nul || set FILES=!FILES! "%%f")
              for /r %%f in (Luau.Common.lib)    do (echo %%f | findstr /i /c:"\CMakeFiles\" >nul || set FILES=!FILES! "%%f")
              for /r %%f in (Luau.Compiler.lib)  do (echo %%f | findstr /i /c:"\CMakeFiles\" >nul || set FILES=!FILES! "%%f")
              for /r %%f in (Luau.Config.lib)    do (echo %%f | findstr /i /c:"\CMakeFiles\" >nul || set FILES=!FILES! "%%f")
              for /r %%f in (Luau.VM.lib)        do (echo %%f | findstr /i /c:"\CMakeFiles\" >nul || set FILES=!FILES! "%%f")
              for /r %%f in (Luau.CodeGen.lib)   do (echo %%f | findstr /i /c:"\CMakeFiles\" >nul || set FILES=!FILES! "%%f")
            )
            if "!FILES!"=="" (
              echo No .lib files found to merge. Failing.
              exit /b 1
            )
            echo Merging libs:
            echo !FILES!
            lib.exe /OUT:luau.lib !FILES!
            if errorlevel 1 exit /b %errorlevel%

            mkdir ..\built_lib 2>nul || exit /b 0
            copy /Y luau.lib ..\built_lib\luau.lib

            echo Checking built_lib contents:
            dir ..\built_lib

            powershell -Command "New-Item -ItemType Directory -Path '..\\archives' -Force"
            powershell -Command ^
              "if ((Test-Path '..\\built_lib') -and (Get-ChildItem '..\\built_lib' -File)) {" ^
              "  Compress-Archive -Path '..\\built_lib\\*' -DestinationPath '..\\archives\\luau-libs-${{ matrix.platform }}-${{ matrix.arch }}.zip' -Force" ^
              "} else {" ^
              "  Write-Error 'built_lib missing or empty'; exit 1" ^
              "}"

            echo Files in archives directory:
            dir ..\archives\
        shell: cmd

      - name: Validate Windows library (luau.lib)
        if: matrix.platform == 'Windows'
        shell: cmd
        run: |
          setlocal EnableDelayedExpansion
          cd project\luau\built_lib
          if not exist "luau.lib" (
            echo ERROR: luau.lib not found after build
            dir
            exit /b 1
          )
          lib.exe /NOLOGO /LIST "luau.lib" >nul 2>&1
          if errorlevel 1 (
            echo ERROR: lib.exe failed to read luau.lib; invalid or corrupt.
            exit /b 1
          )
          where dumpbin >nul 2>&1 && dumpbin /headers "luau.lib" >nul 2>&1

      - name: Build Luau library (MinGW)
        if: matrix.platform == 'MinGW'
        shell: bash
        run: |
          cd project/luau
          mkdir -p build
          cd build

          sudo apt-get update
          sudo apt-get install -y mingw-w64 cmake ninja-build

          if [ "${{ matrix.arch }}" = "x64" ]; then
            cmake .. -G "Ninja" -DCMAKE_BUILD_TYPE=MinSizeRel -DLUAU_BUILD_TESTS=OFF -DLUAU_BUILD_CLI=OFF \
              -DCMAKE_SYSTEM_NAME=Windows \
              -DCMAKE_SYSTEM_PROCESSOR=x86_64 \
              -DCMAKE_C_COMPILER=x86_64-w64-mingw32-gcc \
              -DCMAKE_CXX_COMPILER=x86_64-w64-mingw32-g++ \
              -DCMAKE_AR=/usr/bin/x86_64-w64-mingw32-ar \
              -DCMAKE_RANLIB=/usr/bin/x86_64-w64-mingw32-ranlib
          else
            cmake .. -G "Ninja" -DCMAKE_BUILD_TYPE=MinSizeRel -DLUAU_BUILD_TESTS=OFF -DLUAU_BUILD_CLI=OFF \
              -DCMAKE_SYSTEM_NAME=Windows \
              -DCMAKE_SYSTEM_PROCESSOR=i686 \
              -DCMAKE_C_COMPILER=i686-w64-mingw32-gcc \
              -DCMAKE_CXX_COMPILER=i686-w64-mingw32-g++ \
              -DCMAKE_AR=/usr/bin/i686-w64-mingw32-ar \
              -DCMAKE_RANLIB=/usr/bin/i686-w64-mingw32-ranlib
          fi

          cmake --build . --config MinSizeRel

          echo "create libluau.a" > combine.mri
          find . -name 'libLuau.Ast.a'      -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
          find . -name 'libLuau.Bytecode.a'  -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
          find . -name 'libLuau.Common.a'    -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
          find . -name 'libLuau.Compiler.a'  -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
          find . -name 'libLuau.Config.a'    -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
          find . -name 'libLuau.VM.a'        -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
          find . -name 'libLuau.CodeGen.a'   -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
          echo "save" >> combine.mri
          echo "end" >> combine.mri
          ar -M < combine.mri
          ranlib libluau.a

          mkdir -p ../built_lib
          cp libluau.a ../built_lib/libluau.a || true

          mkdir -p ../archives
          if [ -d ../built_lib ] && [ "$(ls -A ../built_lib || true)" ]; then
            tar -C ../built_lib -czf ../archives/luau-libs-${{ matrix.platform }}-${{ matrix.arch }}.tar.gz .
            echo "Created archive: ../archives/luau-libs-${{ matrix.platform }}-${{ matrix.arch }}.tar.gz"
            ls -la ../archives
          else
            echo "ERROR: built_lib empty or missing"; ls -la .. || true; exit 1
          fi

      - name: Validate MinGW library
        if: matrix.platform == 'MinGW'
        shell: bash
        run: |
          set -e
          cd project/luau/built_lib
          if [ ! -f "libluau.a" ]; then
            echo "ERROR: libluau.a not found after build"
            ls -la
            exit 1
          fi
          ar -t libluau.a > /dev/null || { echo "ERROR: 'ar' failed to read libluau.a"; exit 1; }

      - name: Upload built libraries (single archive)
        uses: actions/upload-artifact@v4
        with:
          name: luau-libs-${{ matrix.platform }}-${{ matrix.arch }}
          path: |
            project/luau/archives/luau-libs-${{ matrix.platform }}-${{ matrix.arch }}.tar.gz
            project/luau/archives/luau-libs-${{ matrix.platform }}-${{ matrix.arch }}.zip
          if-no-files-found: error

  build-android-libs:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm64
            android_abi: arm64-v8a
            lib_name: libluau-arm64.a
          - arch: x64
            android_abi: x86_64
            lib_name: libluau-x86_64.a
          - arch: armv7a
            android_abi: armeabi-v7a
            lib_name: libluau-armv7a.a
          - arch: x86
            android_abi: x86
            lib_name: libluau-x86.a
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Clone Luau repository
        run: |
          rm -rf project/luau || true
          git clone https://github.com/luau-lang/luau.git project/luau
        shell: bash

      - name: Build Luau library (Android)
        run: |
          cd project/luau
          mkdir -p build
          cd build

          sudo apt-get update
          sudo apt-get install -y cmake ninja-build

          if [ -n "$ANDROID_NDK_LATEST_HOME" ]; then NDK="$ANDROID_NDK_LATEST_HOME"; else NDK="$ANDROID_NDK_HOME"; fi

          cmake .. -G "Ninja" \
            -DCMAKE_BUILD_TYPE=MinSizeRel \
            -DCMAKE_TOOLCHAIN_FILE="$NDK/build/cmake/android.toolchain.cmake" \
            -DANDROID_ABI="${{ matrix.android_abi }}" \
            -DANDROID_PLATFORM=android-24 \
            -DANDROID_STL=c++_static \
            -DLUAU_BUILD_TESTS=OFF -DLUAU_BUILD_CLI=OFF

          cmake --build . --config MinSizeRel

          echo "Listing libLuau.*.a recursively:"
          find . -name 'libLuau.Ast.a'      -type f -print || true
          find . -name 'libLuau.Bytecode.a'  -type f -print || true
          find . -name 'libLuau.Common.a'    -type f -print || true
          find . -name 'libLuau.Compiler.a'  -type f -print || true
          find . -name 'libLuau.Config.a'    -type f -print || true
          find . -name 'libLuau.VM.a'        -type f -print || true

          echo "create ${{ matrix.lib_name }}" > combine.mri
          find . -name 'libLuau.Ast.a'      -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
          find . -name 'libLuau.Bytecode.a'  -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
          find . -name 'libLuau.Common.a'    -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
          find . -name 'libLuau.Compiler.a'  -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
          find . -name 'libLuau.Config.a'    -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
          find . -name 'libLuau.VM.a'        -type f -print0 | xargs -0 -I{} echo "addlib {}" >> combine.mri
          echo "save" >> combine.mri
          echo "end" >> combine.mri
          ar -M < combine.mri
          ranlib "${{ matrix.lib_name }}" || true

          mkdir -p ../built_lib
          cp "${{ matrix.lib_name }}" ../built_lib/ || true
          ls -la ../built_lib/

          mkdir -p ../archives
          if [ -d ../built_lib ] && [ "$(ls -A ../built_lib || true)" ]; then
            tar -C ../built_lib -czf ../archives/luau-libs-Android-${{ matrix.arch }}.tar.gz .
            echo "Created archive: ../archives/luau-libs-Android-${{ matrix.arch }}.tar.gz"
            ls -la ../archives
          else
            echo "ERROR: built_lib empty or missing"; ls -la .. || true; exit 1
          fi
        shell: bash

      - name: Upload built libraries
        uses: actions/upload-artifact@v4
        with:
          name: luau-libs-Android-${{ matrix.arch }}
          path: project/luau/archives/luau-libs-Android-${{ matrix.arch }}.tar.gz
          if-no-files-found: error

  build-ios-libs:
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm64
            sdk: iphoneos
            lib_name: libluau_device.a
          - arch: x64
            sdk: iphonesimulator
            lib_name: libluau_sim.a
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup CMake
        uses: jwlawson/actions-setup-cmake@v1.14
        with:
          cmake-version: '3.22.x'

      - name: Clone Luau repository
        run: |
          rm -rf project/luau || true
          git clone https://github.com/luau-lang/luau.git project/luau
        shell: bash

      - name: Build Luau library (iOS)
        run: |
          cd project/luau
          mkdir -p build
          cd build

          if [ "${{ matrix.arch }}" = "x64" ]; then
            cmake .. -G "Unix Makefiles" -DCMAKE_BUILD_TYPE=MinSizeRel -DCMAKE_SYSTEM_NAME=iOS -DCMAKE_OSX_SYSROOT=${{ matrix.sdk }} -DCMAKE_OSX_ARCHITECTURES=x86_64 -DCMAKE_OSX_DEPLOYMENT_TARGET=13.0 -DLUAU_BUILD_TESTS=OFF -DLUAU_BUILD_CLI=OFF -DCMAKE_EXE_LINKER_FLAGS="-framework CoreFoundation"
          else
            cmake .. -G "Unix Makefiles" -DCMAKE_BUILD_TYPE=MinSizeRel -DCMAKE_SYSTEM_NAME=iOS -DCMAKE_OSX_SYSROOT=${{ matrix.sdk }} -DCMAKE_OSX_ARCHITECTURES=arm64 -DCMAKE_OSX_DEPLOYMENT_TARGET=13.0 -DLUAU_BUILD_TESTS=OFF -DLUAU_BUILD_CLI=OFF -DCMAKE_EXE_LINKER_FLAGS="-framework CoreFoundation"
          fi

          cmake --build . --config MinSizeRel

          libs=$( (
            find . -name 'libLuau.Ast.a'     -type f
            find . -name 'libLuau.Bytecode.a' -type f
            find . -name 'libLuau.Common.a'   -type f
            find . -name 'libLuau.Compiler.a' -type f
            find . -name 'libLuau.Config.a'   -type f
            find . -name 'libLuau.VM.a'       -type f
            find . -name 'libLuau.CodeGen.a'  -type f
          ) | tr '\n' ' ' )
          if [ -z "$libs" ]; then
            echo "No libLuau.*.a found"; exit 1
          fi
          libtool -static -o "${{ matrix.lib_name }}" $libs
          ranlib "${{ matrix.lib_name }}"

          mkdir -p ../built_lib
          cp "${{ matrix.lib_name }}" ../built_lib/ || true
          ls -la ../built_lib/

          mkdir -p ../archives
          if [ -d ../built_lib ] && [ "$(ls -A ../built_lib || true)" ]; then
            tar -C ../built_lib -czf ../archives/luau-libs-iOS-${{ matrix.arch }}.tar.gz .
            echo "Created archive: ../archives/luau-libs-iOS-${{ matrix.arch }}.tar.gz"
            ls -la ../archives
          else
            echo "ERROR: built_lib empty or missing"; ls -la .. || true; exit 1
          fi
        shell: bash

      - name: Upload built libraries
        uses: actions/upload-artifact@v4
        with:
          name: luau-libs-iOS-${{ matrix.arch }}
          path: project/luau/archives/luau-libs-iOS-${{ matrix.arch }}.tar.gz
          if-no-files-found: error

  combine-libs:
    needs: [build-libs, build-android-libs, build-ios-libs]
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Create lib directories
        run: |
          rm -rf project/luau
          mkdir -p project/luau/include
          mkdir -p project/luau/lib/Linux/x64
          mkdir -p project/luau/lib/Linux/x86
          mkdir -p project/luau/lib/Linux/arm64
          mkdir -p project/luau/lib/Linux/armv7
          mkdir -p project/luau/lib/Windows/x64
          mkdir -p project/luau/lib/Windows/x86
          mkdir -p project/luau/lib/Windows/arm64
          mkdir -p project/luau/lib/MinGW/x64
          mkdir -p project/luau/lib/MinGW/x86
          mkdir -p project/luau/lib/MacOS/arm64
          mkdir -p project/luau/lib/MacOS/x64
          mkdir -p project/luau/lib/Android
          mkdir -p project/luau/lib/iOS

      - name: Fetch Luau headers
        run: |
          git clone --depth 1 --filter=blob:none --sparse https://github.com/luau-lang/luau.git /tmp/luau-src
          cd /tmp/luau-src
          git sparse-checkout set VM/include Compiler/include CodeGen/include
          cd "$OLDPWD"
          cp /tmp/luau-src/VM/include/*.h project/luau/include/
          cp /tmp/luau-src/Compiler/include/*.h project/luau/include/
          cp /tmp/luau-src/CodeGen/include/*.h project/luau/include/
          rm -rf /tmp/luau-src
        shell: bash

      - name: Extract artifact archives (tar.gz / zip) into their artifact folders
        shell: bash
        run: |
          shopt -s nullglob
          for d in luau-libs-* ; do
            if [ -d "$d" ]; then
              echo "Processing artifact folder: $d"
              for a in "$d"/*; do
                case "$a" in
                  *.tar.gz) echo "Extracting $a"; tar -xzf "$a" -C "$d" ;;
                  *.zip) echo "Unzipping $a"; unzip -q "$a" -d "$d" ;;
                  *) echo "Skipping non-archive file $a" ;;
                esac
              done
              echo "Contents of $d after extraction:"
              ls -la "$d" || true
            fi
          done

      - name: Move extracted files to correct locations
        shell: bash
        run: |
          if [ -d "luau-libs-Linux-x64" ] && [ -f "luau-libs-Linux-x64/libluau.a" ]; then
            mv luau-libs-Linux-x64/libluau.a project/luau/lib/Linux/x64/libluau.a
            echo "Moved Linux x64 library"
          else
            echo "No Linux x64 library found"
          fi
          if [ -d "luau-libs-Linux-arm64" ] && [ -f "luau-libs-Linux-arm64/libluau.a" ]; then
            mv luau-libs-Linux-arm64/libluau.a project/luau/lib/Linux/arm64/libluau.a
            echo "Moved Linux arm64 library"
          else
            echo "No Linux arm64 library found"
          fi
          if [ -d "luau-libs-Linux-x86" ] && [ -f "luau-libs-Linux-x86/libluau.a" ]; then
            mv luau-libs-Linux-x86/libluau.a project/luau/lib/Linux/x86/libluau.a
            echo "Moved Linux x86 library"
          else
            echo "No Linux x86 library found"
          fi
          if [ -d "luau-libs-Linux-armv7" ] && [ -f "luau-libs-Linux-armv7/libluau.a" ]; then
            mv luau-libs-Linux-armv7/libluau.a project/luau/lib/Linux/armv7/libluau.a
            echo "Moved Linux armv7 library"
          else
            echo "No Linux armv7 library found"
          fi
          if [ -d "luau-libs-Windows-x64" ] && [ -f "luau-libs-Windows-x64/luau.lib" ]; then
            mv luau-libs-Windows-x64/luau.lib project/luau/lib/Windows/x64/luau.lib
            echo "Moved Windows x64 library"
          else
            echo "No Windows x64 library found"
          fi
          if [ -d "luau-libs-Windows-arm64" ] && [ -f "luau-libs-Windows-arm64/luau.lib" ]; then
            mv luau-libs-Windows-arm64/luau.lib project/luau/lib/Windows/arm64/luau.lib
            echo "Moved Windows arm64 library"
          else
            echo "No Windows arm64 library found"
          fi
          if [ -d "luau-libs-Windows-x86" ] && [ -f "luau-libs-Windows-x86/luau.lib" ]; then
            mv luau-libs-Windows-x86/luau.lib project/luau/lib/Windows/x86/luau.lib
            echo "Moved Windows x86 library"
          else
            echo "No Windows x86 library found"
          fi
          if [ -d "luau-libs-MinGW-x64" ] && [ -f "luau-libs-MinGW-x64/libluau.a" ]; then
            mv luau-libs-MinGW-x64/libluau.a project/luau/lib/MinGW/x64/libluau.a
            echo "Moved MinGW x64 library"
          else
            echo "No MinGW x64 library found"
          fi
          if [ -d "luau-libs-MinGW-x86" ] && [ -f "luau-libs-MinGW-x86/libluau.a" ]; then
            mv luau-libs-MinGW-x86/libluau.a project/luau/lib/MinGW/x86/libluau.a
            echo "Moved MinGW x86 library"
          else
            echo "No MinGW x86 library found"
          fi
          if [ -d "luau-libs-MacOS-x64" ] && [ -f "luau-libs-MacOS-x64/libluau.a" ]; then
            mv luau-libs-MacOS-x64/libluau.a project/luau/lib/MacOS/x64/libluau.a
            echo "Moved MacOS x64 library"
          else
            echo "No MacOS x64 library found"
          fi
          if [ -d "luau-libs-MacOS-arm64" ] && [ -f "luau-libs-MacOS-arm64/libluau.a" ]; then
            mv luau-libs-MacOS-arm64/libluau.a project/luau/lib/MacOS/arm64/libluau.a
            echo "Moved MacOS arm64 library"
          else
            echo "No MacOS arm64 library found"
          fi
          if [ -d "luau-libs-Android-arm64" ] && [ -f "luau-libs-Android-arm64/libluau-arm64.a" ]; then
            mv luau-libs-Android-arm64/libluau-arm64.a project/luau/lib/Android/libluau-arm64.a
            echo "Moved Android arm64 library"
          else
            echo "No Android arm64 library found"
          fi
          if [ -d "luau-libs-Android-x64" ] && [ -f "luau-libs-Android-x64/libluau-x86_64.a" ]; then
            mv luau-libs-Android-x64/libluau-x86_64.a project/luau/lib/Android/libluau-x86_64.a
            echo "Moved Android x64 library"
          else
            echo "No Android x64 library found"
          fi
          if [ -d "luau-libs-Android-armv7a" ] && [ -f "luau-libs-Android-armv7a/libluau-armv7a.a" ]; then
            mv luau-libs-Android-armv7a/libluau-armv7a.a project/luau/lib/Android/libluau-armv7a.a
            echo "Moved Android armv7a library"
          else
            echo "No Android armv7a library found"
          fi
          if [ -d "luau-libs-Android-x86" ] && [ -f "luau-libs-Android-x86/libluau-x86.a" ]; then
            mv luau-libs-Android-x86/libluau-x86.a project/luau/lib/Android/libluau-x86.a
            echo "Moved Android x86 library"
          else
            echo "No Android x86 library found"
          fi
          if [ -d "luau-libs-iOS-arm64" ] && [ -f "luau-libs-iOS-arm64/libluau_device.a" ]; then
            mv luau-libs-iOS-arm64/libluau_device.a project/luau/lib/iOS/libluau_device.a
            echo "Moved iOS device (arm64) library"
          else
            echo "No iOS arm64 library found"
          fi
          if [ -d "luau-libs-iOS-x64" ] && [ -f "luau-libs-iOS-x64/libluau_sim.a" ]; then
            mv luau-libs-iOS-x64/libluau_sim.a project/luau/lib/iOS/libluau_sim.a
            echo "Moved iOS simulator (x64) library"
          else
            echo "No iOS x64 library found"
          fi

          rm -rf luau-libs-*

      - name: Push to GitHub
        uses: stefanzweifel/git-auto-commit-action@master
        with:
          commit_message: "Update `Luau` libs and headers."
